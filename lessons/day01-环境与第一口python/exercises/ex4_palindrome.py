def is_palindrome(s: str) -> bool:
    """判断 s 是否回文，忽略大小写与非字母数字字符。

    "A man, a plan, a canal: Panama" -> True。
    提示：先把字符标准化（只留字母数字、转小写），再用切片 s[::-1] 反转比较。
    用 str 的 isalnum() 和 lower() 方法。
    """
    # TODO: 实现回文判断
    raise NotImplementedError
