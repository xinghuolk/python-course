def unique_preserve_order(items: list[str]) -> list[str]:
    """对字符串列表保序去重。

    set 可以快速判断是否见过某个元素，但 set 本身不保证按输入顺序输出。
    因此这里需要 set + list 配合。
    """
    # TODO: 用 seen set 辅助生成结果列表
    raise NotImplementedError
