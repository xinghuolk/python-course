def filter_even_numbers(numbers: list[int]) -> list[int]:
    """返回 numbers 中所有偶数，保持原顺序。

    要求：
    - 不修改原列表。
    - 0 也是偶数。
    - 可以用普通 for，也可以用 list comprehension。
    """
    # TODO: 实现筛选逻辑
    raise NotImplementedError
