def normalize_names(names: list[str]) -> list[str]:
    """清洗名字列表。

    对每个名字：
    - 去掉首尾空白；
    - 转成小写；
    - 把中间连续空白压成一个空格；
    - 清洗后为空字符串的项丢弃。

    示例：[" Alice ", "BOB", "  "] -> ["alice", "bob"]
    """
    # TODO: 实现字符串清洗
    raise NotImplementedError
