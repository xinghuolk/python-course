def top_students(records: list[dict[str, object]], threshold: int) -> list[str]:
    """从学生记录中找出分数大于等于 threshold 的学生名字。

    records 的元素形如：{"name": "Alice", "score": 92}

    要求：
    - 返回名字列表；
    - 保持原 records 顺序；
    - 缺少 name 或 score 的记录忽略。
    """
    # TODO: 遍历 list[dict] 并过滤
    raise NotImplementedError
