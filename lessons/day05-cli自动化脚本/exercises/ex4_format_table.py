def format_table(rows: list[tuple[str, int]]) -> str:
    """把 (name, count) 列表格式化成对齐的多行字符串，每行 "name: count"，
    其中 name 左对齐补空格到最长 name 的宽度。空列表返回空字符串。

    例：[("a", 1), ("bb", 22)] -> "a : 1\\nbb: 22"
    """
    # TODO: 计算列宽并格式化
    raise NotImplementedError
