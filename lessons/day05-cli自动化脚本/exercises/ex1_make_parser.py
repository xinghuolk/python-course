import argparse


def make_parser() -> argparse.ArgumentParser:
    """构造一个命令行参数解析器并返回（不要在这里调用 parse_args）：

    - 位置参数 path（字符串）
    - 可选 --limit，int 类型，默认 10
    - 可选 --verbose，开关（action="store_true"），默认 False
    """
    # TODO: 用 argparse 构造 parser
    raise NotImplementedError
